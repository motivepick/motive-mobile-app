import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    colorTagCircle: {
        width: 12,
        height: 12,
        borderRadius: 12 / 2,
        marginLeft: 20
    }
})

export default styles
